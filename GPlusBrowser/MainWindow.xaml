<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
        xmlns:ei="http://schemas.microsoft.com/expression/2010/interactions"
        xmlns:local="clr-namespace:GPlusBrowser"
        xmlns:localVm="clr-namespace:GPlusBrowser.ViewModel"
        xmlns:localCtrl="clr-namespace:GPlusBrowser.Controls"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d" x:Class="GPlusBrowser.MainWindow"
        Title="GPlusBrowser" Height="700" Width="500" x:Name="mainWindow">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="booleanToVisibilityConverter" />
        <Style x:Key="PageSwitcherStyle" TargetType="{x:Type TabControl}">
            <Setter Property="Padding" Value="0"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TabControl}">
                        <DockPanel x:Name="templateRoot" ClipToBounds="true" SnapsToDevicePixels="true" KeyboardNavigation.TabNavigation="Local">
                            <TabPanel x:Name="headerPanel" Height="0" DockPanel.Dock="Top" Visibility="Hidden" Background="Transparent" IsItemsHost="true" />
                            <Border x:Name="contentPanel" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}" KeyboardNavigation.DirectionalNavigation="Contained" KeyboardNavigation.TabIndex="2" KeyboardNavigation.TabNavigation="Local">
                                <ContentPresenter x:Name="PART_SelectedContentHost" ContentSource="SelectedContent" Margin="{TemplateBinding Padding}" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                            </Border>
                        </DockPanel>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="postItemTemplate" TargetType="{x:Type TreeViewItem}">
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TreeViewItem}">
                        <Grid Margin="0">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition />
                            </Grid.RowDefinitions>
                            <ContentPresenter Content="{TemplateBinding HeaderedContentControl.Header}" ContentTemplate="{TemplateBinding HeaderedContentControl.HeaderTemplate}" ContentStringFormat="{TemplateBinding HeaderStringFormat}" ContentSource="Header" x:Name="PART_Header" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                            <ItemsPresenter x:Name="ItemsHost" Margin="15,0,0,0" Grid.Row="1" />
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="iconButtonTemplate" TargetType="{x:Type Button}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <ContentPresenter />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <DataTemplate x:Key="accountTemplate">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <!--<ColumnDefinition Width="*" />-->
                    <!--通知類パネル-->
                </Grid.ColumnDefinitions>
                <Grid Grid.Column="0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="55" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="55" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Grid Grid.Row="0" Grid.Column="0" Grid.RowSpan="2" Grid.ColumnSpan="2" Width="200" Height="55" VerticalAlignment="Top" HorizontalAlignment="Left">
                        <Grid.OpacityMask>
                            <RadialGradientBrush Center="0,0" RadiusX="1" RadiusY="1" GradientOrigin="0,0">
                                <GradientStop Offset="0" Color="#a0000000" />
                                <GradientStop Offset="1" Color="#00000000" />
                            </RadialGradientBrush>
                        </Grid.OpacityMask>
                        <Image Width="200" Height="55" VerticalAlignment="Top" HorizontalAlignment="Left" Stretch="UniformToFill" Source="{Binding UserIconUrl}" />
                    </Grid>
                    <Button Grid.Row="0" Grid.Column="0" Width="30" Height="30" Margin="10" Style="{StaticResource iconButtonTemplate}" Command="{Binding BackToAccountManagerCommand}">
                        <Image Source="/Resources/back.png" />
                    </Button>
                    <Grid Grid.Row="0" Grid.Column="1">
                        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Left" Orientation="Horizontal">
                            <Image Width="35" Height="35" Margin="0,0,10,0" VerticalAlignment="Center" Source="{Binding UserIconUrl}" />
                            <TextBlock FontSize="21" Margin="0,0,10,0" VerticalAlignment="Center"><Run Text="Stream/"/></TextBlock>
                            <ComboBox MinWidth="150" VerticalAlignment="Center" FontSize="16" SelectedIndex="{Binding Stream.SelectedCircleIndex}" DisplayMemberPath="CircleName" ItemsSource="{Binding Stream.DisplayStreams}" />
                        </StackPanel>
                    </Grid>
                    <TabControl Grid.Row="1" Grid.Column="1" ItemsSource="{Binding Stream.DisplayStreams, Mode=OneWay}" SelectedIndex="{Binding Stream.SelectedCircleIndex}" Style="{StaticResource PageSwitcherStyle}" />
                </Grid>
                <ContentPresenter Grid.Column="1" Content="{Binding Notification}" />
            </Grid>
        </DataTemplate>
        <DataTemplate DataType="{x:Type localVm:AccountSelectorViewModel}">
            <Grid Grid.ColumnSpan="2" Background="White">
                <Grid.RowDefinitions>
                    <RowDefinition Height="55" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="20,5,5,0" FontSize="21"><Run Text="Account"/></TextBlock>
                <StackPanel Grid.Row="1">
                    <ItemsControl ItemsSource="{Binding Accounts}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel IsItemsHost="True" />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate DataType="{x:Type localVm:AccountViewModel}">
                                <DockPanel Margin="20,0,15,15">
                                    <WrapPanel DockPanel.Dock="Bottom" Margin="15,10,0,0">
                                        <Button Padding="8,5,8,5" Command="{Binding OpenStreamPanelCommand}" Content="ストリームを開く"/>
                                    </WrapPanel>
                                    <Image DockPanel.Dock="Left" Width="35" Height="35" Margin="0" HorizontalAlignment="Left" Source="{Binding UserIconUrl}" />
                                    <TextBlock Padding="0" Margin="10,0,0,0" VerticalAlignment="Center" Text="{Binding UserName}" FontSize="18" />
                                </DockPanel>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Grid>
        </DataTemplate>
        <DataTemplate DataType="{x:Type localVm:AttachedLinkViewModel}">
            <Border BorderBrush="LightGray" BorderThickness="0,1,0,0" Margin="0,8,0,8">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <!--<Image Grid.Column="0" Source="{Binding ThumnailUrl}" MaxWidth="150" MaxHeight="150" Stretch="Uniform" VerticalAlignment="Top" Margin="0,8,8,0" Visibility="{Binding ExistThumnailUrl, Converter={StaticResource booleanToVisibilityConverter}}" />-->
                    <TextBlock Grid.Column="1" Margin="0,8,0,0" TextWrapping="Wrap"><Hyperlink><i:Interaction.Triggers>
                                                    <i:EventTrigger EventName="Click">
                                                        <ei:LaunchUriOrFileAction Path="{Binding AncourUrl}" />
                                                    </i:EventTrigger>
                                                </i:Interaction.Triggers><Run FontWeight="Bold" Text="{Binding AncourTitle}" /></Hyperlink><LineBreak /><Run Text="{Binding AncourIntroductionText}" /></TextBlock>
                </Grid>
            </Border>
        </DataTemplate>
        <DataTemplate DataType="{x:Type localVm:AttachedAlbumViewModel}">
            <localCtrl:ImageBox Height="300" ItemsSource="{Binding ThumbnailImages}" Content="{Binding SelectedImage}" />
        </DataTemplate>
        <DataTemplate DataType="{x:Type localVm:CommentViewModel}">
            <Border BorderBrush="LightGray" BorderThickness="0,1,0,0" Padding="5">
                <DockPanel Margin="0" Focusable="False">
                    <TextBlock DockPanel.Dock="Bottom" Margin="0,3,0,3" local:InlineBehavior.Inline="{Binding PostContentInline}" TextWrapping="Wrap" />
                    <Image DockPanel.Dock="Left" Width="25" Height="25" SnapsToDevicePixels="True" Source="{Binding OwnerIconUrl}" />
                    <TextBlock DockPanel.Dock="Left" Margin="5,0,0,0" VerticalAlignment="Center"><Run Text="{Binding OwnerName}" /><Run Text=" "/><Run Foreground="Gray" Text="{Binding CommentDate, StringFormat=- \{0\} -}" /></TextBlock>

                </DockPanel>
            </Border>
        </DataTemplate>
        <DataTemplate DataType="{x:Type localVm:ActivityViewModel}">
            <DataTemplate.Resources>
                <localCtrl:IsExpandCommentCountToStringConverter x:Key="commentExpanderHeaderConverter" />
            </DataTemplate.Resources>
            <Border BorderBrush="LightGray" BorderThickness="0,1,0,0" Padding="5">
                <DockPanel Margin="0" Focusable="False">
                    <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0">
                        <Image Width="25" Height="25" SnapsToDevicePixels="True" Source="{Binding PostUserIconUrl}" />
                        <TextBlock Margin="5,0,0,0" VerticalAlignment="Center">
                            <Run Text="{Binding PostUserName}" />
                            <Run Text=" "/>
                                <Span Foreground="Gray">
                                    <Run Text="- "/>
                                        <Hyperlink TextDecorations="None" Foreground="Gray">
                                            <i:Interaction.Triggers>
                                                <i:EventTrigger EventName="Click">
                                                    <ei:LaunchUriOrFileAction Path="{Binding ActivityUrl}" />
                                                </i:EventTrigger>
                                                <i:EventTrigger EventName="MouseEnter">
                                                    <ei:ChangePropertyAction PropertyName="TextDecorations" Value="Underline" />
                                                </i:EventTrigger>
                                                <i:EventTrigger EventName="MouseLeave">
                                                    <ei:ChangePropertyAction PropertyName="TextDecorations" Value="None" />
                                                </i:EventTrigger>
                                            </i:Interaction.Triggers>
                                            <Run Text="{Binding PostDate}" />
                                        </Hyperlink>
                                    <Run Text=" -"/>
                                </Span>
                        </TextBlock>
                    </StackPanel>
                    <TextBlock DockPanel.Dock="Top" Margin="0,3,0,3" local:InlineBehavior.Inline="{Binding PostContentInline}" TextWrapping="Wrap" />
                    <ContentPresenter DockPanel.Dock="Top" Content="{Binding AttachedContent}" />
                    <localCtrl:ExpandableListView ItemsSource="{Binding Comments}" Margin="15,10,0,10" HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch">
                        <localCtrl:ExpandableListView.Header>
                            <MultiBinding Converter="{StaticResource commentExpanderHeaderConverter}">
                                <Binding RelativeSource="{RelativeSource Self}" Path="IsExpand" />
                                <Binding RelativeSource="{RelativeSource Self}" Path="Items.Count" />
                            </MultiBinding>
                        </localCtrl:ExpandableListView.Header>
                    </localCtrl:ExpandableListView>
                </DockPanel>
            </Border>
        </DataTemplate>
        <DataTemplate DataType="{x:Type localVm:StreamViewModel}">
            <TreeView
                Focusable="False" ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                VirtualizingPanel.IsVirtualizing="True"
                VirtualizingPanel.VirtualizationMode="Recycling"
                VirtualizingPanel.ScrollUnit="Pixel"
                BorderThickness="0" ItemsSource="{Binding Activities}"
                ItemContainerStyle="{StaticResource postItemTemplate}" />
        </DataTemplate>
    </Window.Resources>
	<Grid>
		<!--各アカウントの画面-->
        <TabControl ItemsSource="{Binding Pages}" SelectedIndex="{Binding SelectedPageIndex, Mode=OneWay}" Style="{StaticResource PageSwitcherStyle}" ContentTemplate="{StaticResource accountTemplate}" />
        <!--アカウント選択画面-->
		<ContentPresenter Visibility="{Binding DataContext.IsAccountSelectorMode, Converter={StaticResource booleanToVisibilityConverter}, RelativeSource={RelativeSource AncestorType={x:Type Window}, Mode=FindAncestor}}" Content="{Binding AccountSelector}" />
	</Grid>
</Window>